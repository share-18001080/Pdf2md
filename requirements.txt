# Bootstrap Solution 1: Requirements - Minimal Base Only
# Layer 1 & 2 use built-in Python, Layer 4+ installs on-demand

# IMMEDIATE INSTALL (Layer 1+2 needs these)
requests>=2.31.0
urllib3>=2.0.0
pypdf>=3.0.0

# NOTE: Marker, Nougat, MinerU installed on-demand via subprocess
# This keeps bootstrap fast and avoids circular dependency